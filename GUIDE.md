# คู่มือการใช้งาน Jangmao70

คู่มือนี้เขียนสำหรับผู้ใช้งานทั่วไปที่ต้องการติดตั้งโปรแกรม กรอกข้อมูล และสร้างเอกสาร Word โดยไม่จำเป็นต้องรู้เรื่องโค้ด

## ใครต้องทำอะไร

| ผู้ใช้งาน | ทำอะไร |
| --- | --- |
| ผู้กรอกข้อมูล | ดาวน์โหลด Installer, ติดตั้งโปรแกรม, กรอกข้อมูล และตรวจเอกสารก่อนสร้าง |
| ผู้ดูแลงานเอกสาร | เตรียมข้อมูลโรงเรียน ลูกจ้าง กรรมการ TOR งวดเบิก และเลือกเอกสารที่ต้องการ |
| ผู้สนใจ source code | ดาวน์โหลด Source ZIP เพื่อศึกษาเท่านั้น ไม่จำเป็นต่อการติดตั้งหรือใช้งาน |

## ดาวน์โหลดไฟล์

- [ดาวน์โหลดตัวติดตั้ง Jangmao70](./assets/downloads/Jangmao70-Installer.zip)
- [ดาวน์โหลด Source code](./assets/downloads/Jangmao70-Source.zip)

ให้ตรวจชื่อไฟล์ก่อนเปิดทุกครั้ง ต้องเป็นชื่อด้านบนและดาวน์โหลดจาก repository ของ Jangmao70 เท่านั้น

## คำเตือนตอนดาวน์โหลด

Chrome อาจเตือนว่าไฟล์ ZIP ดูน่าสงสัย เช่น เมื่อไฟล์ยังมีผู้ดาวน์โหลดไม่มากหรือระบบยังไม่รู้จักไฟล์ คำเตือนนี้อาจเกิดกับทั้ง Installer และ Source ZIP

ภาพด้านล่างเป็นตัวอย่างคำเตือนจาก jmoney รุ่นก่อน หน้าตาคำเตือนอาจเปลี่ยนตามรุ่น Chrome แต่หลักการตรวจสอบเหมือนกัน

![ตัวอย่างคำเตือนในรายการดาวน์โหลดของ Chrome](./assets/guide/chrome-warning-history.png)

![ตัวอย่างหน้ารายละเอียดคำเตือนของ Chrome](./assets/guide/chrome-warning-detail.png)

เปิดรายการดาวน์โหลด Chrome แล้วคลิกลูกศรหรือรายการไฟล์ที่มีคำเตือน เพื่อเปิดหน้ารายละเอียด

ก่อนเลือกดาวน์โหลดต่อ ให้ตรวจสอบว่า:

1. ดาวน์โหลดจาก repository `RobbyGrean/Jangmao70` จริง
2. ชื่อไฟล์ตรงกับ `Jangmao70-Installer.zip` หรือ `Jangmao70-Source.zip`
3. ไม่ได้มาจากลิงก์ที่ส่งต่อมาโดยไม่ทราบแหล่งที่มา

เมื่อตรวจสอบและเชื่อถือไฟล์แล้ว คลิก **ดาวน์โหลดไฟล์ที่น่าสงสัย** (บางรุ่นใช้ **Keep / เก็บไว้** หรือ **Keep anyway / เก็บไว้ต่อไป**) แล้วรอให้ดาวน์โหลดเสร็จ

ถ้าชื่อไฟล์หรือแหล่งที่มาไม่ตรง ให้ยกเลิกและอย่าเปิดไฟล์

## วิธีติดตั้ง

1. ดาวน์โหลด `Jangmao70-Installer.zip`
2. คลิกขวาไฟล์ ZIP แล้วเลือก **Extract All / แตกไฟล์ทั้งหมด**
3. เปิดโฟลเดอร์ที่แตกออกมา แล้วเปิด `Jangmao70-Setup.exe`
4. รอให้ตัวติดตั้งทำงานจนเสร็จ
5. เปิดโปรแกรมจาก shortcut ชื่อ `Jangmao70` บน Desktop หรือ Start Menu

อย่าเปิด Setup โดยตรงจากด้านใน ZIP และควรปิด Jangmao70 กับไฟล์ Word ที่เกี่ยวข้องก่อนติดตั้งรุ่นใหม่ทับรุ่นเดิม

### Windows protected your PC — More info → Run anyway

1. เมื่อขึ้น **Windows protected your PC** คลิก **More info / ข้อมูลเพิ่มเติม**
2. ตรวจชื่อแอปให้เป็น **Jangmao70-Setup.exe** จากไฟล์ที่ดาวน์โหลดจากเว็บไซต์ทางการ
3. คลิก **Run anyway / เรียกใช้ต่อไป** ที่ปรากฏขึ้น แล้วทำตามหน้าต่างติดตั้ง

![ตัวอย่างสองขั้นตอนบน Windows: คลิก More info แล้วคลิก Run anyway ที่วงไว้](./assets/guide/windows-smartscreen.png)

SmartScreen อาจเตือนเมื่อแอปยังไม่มีชื่อเสียงเพียงพอหรือไม่มีลายเซ็นที่ระบบรู้จัก ทำต่อเฉพาะไฟล์ที่ตรวจสอบและเชื่อถือ หากไม่มีปุ่ม Run anyway ให้ติดต่อผู้ดูแลเครื่อง เพราะนโยบายองค์กรอาจไม่อนุญาต

## เริ่มใช้งาน

เมื่อเปิด Jangmao70 จะมีสองแท็บให้เลือก แต่ละแท็บเป็นงานคนละ workflow และสร้างผลลัพธ์คนละโฟลเดอร์

### แท็บจัดซื้อจัดจ้างและสัญญา

กรอกตามลำดับด้านซ้าย:

1. **ข้อมูลโรงเรียน** — ชื่อโรงเรียน เขตพื้นที่ และผู้รับผิดชอบ
2. **กรรมการ TOR** — กรรมการ 1–3 และเลขที่คำสั่งแต่งตั้ง TOR และตรวจรับ
3. **ข้อมูลลูกจ้าง** — ข้อมูลส่วนตัว ตำแหน่ง เงินเดือน การศึกษา ที่อยู่ และข้อมูลบัตร
4. **สรุปชุดเอกสารที่ต้องการสร้าง** — ตรวจรายการเอกสารก่อนสร้าง

ช่องที่มีเครื่องหมาย `*` เป็นข้อมูลที่ควรกรอกเพื่อให้เอกสารสมบูรณ์ ช่องที่เว้นว่างอาจถูกแทนด้วยจุดตามที่ระบบกำหนด

### แท็บเบิกเงินเดือน

กรอกข้อมูลรอบเบิก ผู้รับจ้าง ตำแหน่ง เงินเดือน วันที่ และข้อมูลผู้ลงนาม จากนั้นเลือกเอกสารที่ต้องการสร้าง

ถ้าใช้คำสั่ง **เหมือนเดิม! แค่เปลี่ยนเดือน!** ระบบจะเสนอเดือนถัดไปจากประวัติการสร้างเอกสารสำเร็จล่าสุดของ Template นั้น ให้ตรวจสอบเดือนและปีทุกครั้งก่อนสร้าง

## โหลดและบันทึก Template

- **บันทึก Template** คือการเก็บข้อมูลที่กรอกไว้ใช้ครั้งต่อไป ไม่ใช่การแก้ไฟล์ Word ต้นฉบับ
- **โหลด Template** คือการนำข้อมูลที่เคยบันทึกกลับมาใส่ในฟอร์ม
- การโหลดข้ามแท็บจะเปิดหน้าต่างให้เลือกว่าจะนำเข้าจากชุดจัดซื้อจัดจ้างและสัญญา หรือจากข้อมูลเบิกเงินเดือนเดิม
- ระบบจะแจ้งข้อมูลที่นำเข้าได้และข้อมูลที่ไม่เกี่ยวข้องหรือยังว่าง ให้ตรวจสอบก่อนสร้างเอกสาร

ข้อมูลที่ใช้ร่วมกันได้ เช่น ชื่อ เลขประชาชน ที่อยู่ และกรรมการ จะถูกนำเข้าตามรายการที่ระบบรองรับ ส่วนงวดเบิก รายการเอกสาร และขั้นตอนเฉพาะของแต่ละแท็บยังคงแยกกัน

## สร้างเอกสาร

1. ตรวจข้อมูลทุก section ให้ครบ
2. ตรวจรายการเอกสารที่เลือก
3. กด **ตรวจสอบและสร้างเอกสาร** หรือ **ตรวจสอบและสร้าง Word**
4. รอจนระบบแจ้งผลสำเร็จ
5. เปิดโฟลเดอร์ Output เพื่อตรวจไฟล์ Word ที่สร้าง

โฟลเดอร์ผลลัพธ์เริ่มต้นอยู่ที่:

```text
%LOCALAPPDATA%\Jangmao70\Output\เอกสารจัดจ้าง
%LOCALAPPDATA%\Jangmao70\Output\เงินเดือน
```

ชื่อไฟล์ผลลัพธ์อิงชื่อผู้รับจ้าง ชื่อ Template และวันที่สร้างเอกสาร หากสร้างซ้ำในวันเดียวกัน ให้ตรวจชื่อไฟล์และไฟล์เดิมก่อนนำไปใช้งาน เพื่อป้องกันการใช้เอกสารผิดชุด

## การปิดโปรแกรมและข้อมูลผู้ใช้

ถ้ามีข้อมูลที่ยังไม่ได้บันทึกก่อนปิด โปรแกรมจะถามว่าจะบันทึกหรือไม่ ให้เลือกตามต้องการ หากกดยกเลิกหรือปิดกล่องคำถาม ข้อมูลในฟอร์มจะยังไม่ถูกทิ้ง

ข้อมูล Template และค่าที่บันทึกไว้เป็นข้อมูลภายในเครื่อง ไม่ได้ส่งขึ้น cloud อัตโนมัติ ควรสำรองข้อมูลก่อนย้ายเครื่องหรือติดตั้งระบบใหม่

## ปัญหาที่พบบ่อย

| อาการ | วิธีตรวจสอบเบื้องต้น |
| --- | --- |
| ดาวน์โหลดแล้ว Chrome เตือน | ตรวจ repository และชื่อไฟล์ก่อนเลือกดาวน์โหลดต่อ |
| Windows ขึ้น SmartScreen | ตรวจแหล่งที่มาและชื่อไฟล์ ห้ามปิดระบบป้องกันทั้งเครื่อง |
| เปิด Setup ไม่ได้ | แตก ZIP ก่อน และตรวจว่ามี `Jangmao70-Setup.exe` อยู่ในโฟลเดอร์ที่แตกแล้ว |
| สร้างเอกสารไม่ครบ | กลับไปตรวจช่องที่มี `*`, รายการเอกสาร และ Template ที่เลือก |
| Word เปิดไฟล์ไม่ได้ | ปิดเอกสาร Word ที่ชื่อเดียวกัน แล้วลองสร้างใหม่ |
| Template หาย | อย่าย้ายหรือลบโฟลเดอร์ Template ภายในโปรแกรม และติดตั้งรุ่นใหม่ทับโดยไม่ลบโฟลเดอร์เดิม |

## ถอนการติดตั้ง

ใช้ shortcut **Uninstall Jangmao70** จาก Start Menu หรือโฟลเดอร์ติดตั้ง

การถอนการติดตั้งจะลบโฟลเดอร์ของ Jangmao70 รวมถึงข้อมูลที่เก็บไว้ใน `Data`, `Template` และ `Output` ภายในโฟลเดอร์นั้น จึงควรสำรองข้อมูลก่อนยืนยัน

## สำหรับผู้สนใจ source code

Source ZIP มี source code, config, template และสคริปต์ build ที่ใช้กับ Jangmao70 ผู้ใช้งานทั่วไปไม่ต้องดาวน์โหลดไฟล์นี้เพื่อเปิดโปรแกรม และไม่ต้องติดตั้ง npm เพื่อใช้งานตัวโปรแกรม

ดูภาพรวมระบบได้จาก:

- [Workflow ของผู้ใช้](./assets/readme/jangmao70-user-workflow.svg)
- [การเชื่อมโยงข้อมูลภายใน](./assets/readme/jangmao70-data-flow.svg)
