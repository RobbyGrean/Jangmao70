# Khet70 configuration scaffold

The Khet70 position catalog, document manifests, shared salary table, template tags, dates, and form fields are included here.
The JSON files are the source configuration used by both modules and are kept separate from the legacy project data.
