# เอกสาร 346 — TAG_MAPPING

วันที่ตรวจ: 2026-09-09  
สถานะ: `VERIFIED` จากไฟล์ DOCX และ `CONFIRMED` ตามคำตอบผู้ใช้ใน `GRILL_HANDOFF.md`; แก้ข้อคลาดเคลื่อนโดย Astra review 2026-09-09  
ขอบเขต: Payroll 5 แบบเดิม + Procurement 34 แบบใน workspace

เอกสารนี้เป็น inventory และสัญญาการจับคู่สำหรับ implementation เท่านั้น รอบ Grill นี้ไม่ได้แก้ DOCX, source หรือ config

## วิธีตรวจและผลรวม

- Procurement ตรวจ `Template/**/*.docx` ครบ 34 ไฟล์ โดยอ่าน `word/document.xml`, `word/header*.xml` และ `word/footer*.xml` แล้วรวมข้อความจาก `<w:t>` ต่อเนื่องเพื่อรองรับ tag ที่ Word แบ่งข้าม runs
- Astra สแกนเฉพาะข้อความ `w:t` ใน document/header/footer พบ **56 business tags / 1,043 occurrences** ของ Procurement; ตัวเลข 1,052 เดิมไม่ตรงกับไฟล์ที่ตรวจรอบนี้ จึงไม่ใช้เป็นเกณฑ์ตรวจรับ ดู hash และวิธีนับใน `ASTRA_REVIEW.md`
- ไม่มี business tag ใน header/footer; ทั้ง 1,043 occurrences ถูกแบ่งข้าม text runs จริง ต้องคงความสามารถแทน tag ข้าม runs และตรวจ formatting แยกจากการนับ tag
- GUID ใน XML attributes/metadata ไม่ใช่ business tags และต้องคงเดิม; ห้ามสแกน raw XML ด้วย brace regex แล้วนับทุกผลเป็นช่องกรอก
- Payroll ตรวจเฉพาะ 5 ไฟล์ตาม manifest ใน baseline `C:\Users\MSI\Documents\App creation\dist\Template`; พบ 34 business tags / 127 occurrences ใน `word/document.xml`, ไม่มี business tag ใน header/footer และทุก occurrence ข้าม runs; ในโฟลเดอร์มี DOCX สำรองอีก 1 ไฟล์ซึ่งไม่อยู่ใน scope
- Payroll registry มีความต่างกับ XML จริง: registry มี 3 tag ที่ไม่พบใน DOCX (`{คำนำหน้าการเงิน}`, `{คำนำหน้าหพัสดุ}`, `{ปีใบสั่งจ้าง}`) และ DOCX จริงมี `{คำนำหน้าชื่อ}` ที่ registry ไม่ได้ประกาศ ต้องรักษา alias/compatibility ตามตารางด้านล่างแล้วปรับ manifest ในงาน implementation

## กลุ่ม path แบบย่อ

Path ทุกตัวเป็น relative path จาก `H:\CodexProjects\346` เว้นแต่ระบุว่าเป็น baseline

| รหัส | Path |
|---|---|
| `PC` | `Template/1.ขอจ้างต่อเนื่อง 630.docx`; `Template/2.1 ขอตั้งกกTOR.docx`; `Template/2.2 คำสั่งTOR.docx`; `Template/4.รายงานผล TOR.docx`; `Template/5. รายงานขอจ้าง.docx`; `Template/7. รายงานผลพิจารณา.docx`; `Template/8. ประกาศผู้ชนะ.docx`; `Template/9. ใบสั่งจ้าง.docx` |
| `PT` | `Template/3.TOR/*.docx` (13 variants) |
| `PQ` | `Template/6. ใบเสนอราคา/*.docx` (13 variants) |
| `PT2` | 4 ไฟล์ `3.1.2`, `3.1.4`, `3.2.2`, `3.2.4` ใน `PT`; รวมทั้ง suffix `-2รร.docx` และ `-สอน2รร.docx` |
| `PQ2` | 4 ไฟล์ `6.1.2`, `6.1.4`, `6.2.2`, `6.2.4` ใน `PQ`; รวมทั้ง suffix `-2รร.docx` และ `-สอน2รร.docx` |
| `PP` | เป้าหมายในรุ่นใหม่ `Template/Payroll/<ชื่อไฟล์>`; ต้นทางที่ตรวจคือ `C:\Users\MSI\Documents\App creation\dist\Template\<ชื่อไฟล์>` |

`PC` เป็นกลุ่มรวมเพื่ออ่านง่าย ไม่ได้หมายความว่าทุกไฟล์ในกลุ่มมีทุก tag; generator ต้องใช้ manifest/scan ของไฟล์ที่เลือกเป็นตัวตัดสินว่าต้องส่งค่าใด

## Procurement — 56 business tags

| Tag | ความหมายและ label ที่ควรใช้ใน UI | Path ที่พบ | Required / conditional | Normalize, ค่าว่าง และ save scope | Alias / สถานะ |
|---|---|---|---|---|---|
| `{ชื่อโรงเรียน}` | ชื่อโรงเรียนของ record การจ้าง | `PC`, `PT`, `PQ` | บังคับก่อนสร้างเอกสาร Procurement | trim และ normalize prefix `โรงเรียน`; จำกับ record การจ้างรายบุคคล | `CONFIRMED` + `VERIFIED` |
| `{ชื่อเขต}` | ชื่อสำนักงานเขตพื้นที่ฯ | `PC` ที่ใช้ tag | บังคับเมื่อ template ที่เลือกใช้ | trim; รายการ default ลำดับที่ 2 `สำนักงานเขตพื้นที่การศึกษาประถมศึกษาประจวบคีรีขันธ์ เขต 2` ตาม context; จำค่าที่เลือกกับ record | `CONFIRMED` + `VERIFIED` |
| `{คำนำหน้าผอ}`, `{ชื่อผอ}`, `{นามสกุลผอ}` | คำนำหน้า/ชื่อ/นามสกุลผู้อำนวยการโรงเรียน | `PC` ที่ใช้ tag | ต้องมีเมื่อเอกสารที่เลือกต้องลงชื่อผอ | trim; prefix เลือก/พิมพ์ตามรายการ; เป็นข้อมูลโรงเรียน/ผู้ลงนาม | `VERIFIED`; ไม่ใช้แทนกรรมการ |
| `{คำนำหน้าพัสดุ}`, `{ชื่อพัสดุ}`, `{นามสกุลพัสดุ}` | คำนำหน้า/ชื่อ/นามสกุลเจ้าหน้าที่พัสดุ | `PC` และไฟล์ที่ใช้ tag | ต้องมีเมื่อเอกสารที่เลือกต้องลงชื่อพัสดุ | trim; เก็บเป็นข้อมูลผู้ลงนามของ record/โรงเรียน | `VERIFIED` |
| `{คำนำหน้าหพัสดุ}`, `{ชื่อหพัสดุ}`, `{นามสกุลหพัสดุ}` | คำนำหน้า/ชื่อ/นามสกุลหัวหน้าพัสดุ | `PC` ที่ใช้ tag | ต้องมีเมื่อเอกสารที่เลือกต้องลงชื่อหัวหน้าพัสดุ | trim; ไม่แสดงชื่อ tag ใน UI | `VERIFIED` จากชื่อ tag/ตำแหน่งใน template |
| `{คำนำหน้าลูกจ้าง}`, `{ชื่อลูกจ้าง}`, `{นามสกุลลูกจ้าง}` | คำนำหน้า/ชื่อ/นามสกุลลูกจ้าง; UI ใช้ label ภาษาคน | `PC`, `PT`, `PQ` ที่ใช้ tag | ชื่อและนามสกุลบังคับ; คำนำหน้าเป็น field ของชื่อ | trim; ชื่อ–นามสกุลจำกับ record ลูกจ้างรายบุคคล | `CONFIRMED` + `VERIFIED` |
| `{ชื่อโรงเรียนสอง}` | ชื่อโรงเรียนที่สองของการทำงาน | `PT2`, `PQ2` | แสดงและบังคับเฉพาะเมื่อเลือกทำงาน 2 โรงเรียน | trim และเติม prefix `โรงเรียน` เพียงครั้งเดียว; ไม่ส่งค่าเมื่อไม่ติ๊ก | `CONFIRMED` + `VERIFIED` |
| `{คำนำหน้าสเปคA}`, `{คำนำหน้าสเปคB}`, `{คำนำหน้าสเปคC}` | คำนำหน้ากรรมการกำหนด TOR A/B/C | `Template/2.1 ขอตั้งกกTOR.docx`, `Template/2.2 คำสั่งTOR.docx`, `PT`, `Template/4.รายงานผล TOR.docx` | กรรมการ A/B/C ต้องครบทั้ง 3 คน; A เป็นประธานอยู่บนสุด | trim; ใช้ชุดกรรมการต้นทางเดียวกับ Payroll `{กรรมการA/B/C}` | `CONFIRMED` + `VERIFIED`; map A→A/B→B/C→C |
| `{ชื่อสเปคA}`, `{ชื่อสเปคB}`, `{ชื่อสเปคC}` | ชื่อกรรมการ TOR A/B/C | paths เดียวกับแถวคำนำหน้าสเปค | บังคับครบทั้ง 3 คน | trim; save เป็นข้อมูลกรรมการชุดร่วม | `CONFIRMED` + `VERIFIED` |
| `{นามสกุลสเปคA}`, `{นามสกุลสเปคB}`, `{นามสกุลสเปคC}` | นามสกุลกรรมการ TOR A/B/C | paths เดียวกับแถวคำนำหน้าสเปค | บังคับครบทั้ง 3 คน | trim; save เป็นข้อมูลกรรมการชุดร่วม | `CONFIRMED` + `VERIFIED` |
| `{ตำแหน่งสเปคA}`, `{ตำแหน่งสเปคB}`, `{ตำแหน่งสเปคC}` | ตำแหน่งกรรมการ TOR A/B/C | paths เดียวกับแถวคำนำหน้าสเปค | บังคับครบทั้ง 3 คน | trim; dropdown ตามรายการที่ยืนยันและพิมพ์เองได้ตามข้อกำหนด; save เป็นข้อมูลกรรมการชุดร่วม | `CONFIRMED` + `VERIFIED` |
| `{คำสั่งสเปค}` | เลข/ข้อความคำสั่งแต่งตั้งชุดกรรมการ TOR | `Template/2.2 คำสั่งTOR.docx`, `Template/4.รายงานผล TOR.docx` | optional | trim; ว่างให้ renderer ส่งจุดว่าง; จำกับ record การจ้างรายบุคคล | `CONFIRMED` + `VERIFIED` |
| `{ตำแหน่ง}` | ตำแหน่งงานของลูกจ้าง | `PC`, `PT`, `PQ` ที่ใช้ tag | บังคับ | ใช้ catalog 5 ตำแหน่งใน context; เป็น source ให้เงินและ routing | `CONFIRMED` + `VERIFIED` |
| `{เงินเดือน}` | เงินเดือนตามตำแหน่ง | `PC`, `PT`, `PQ` ที่ใช้ tag | computed จากตำแหน่งก่อนสร้าง | static mapping ตามตำแหน่ง; ไม่ให้ค่าค้างจากตำแหน่งเดิม | `CONFIRMED` + `VERIFIED` |
| `{เงินเดือนTEXT}` | คำอ่านเงินเดือน | `PC`, `PT`, `PQ` ที่ใช้ tag | computed | static mapping คู่กับเงินเดือน | `CONFIRMED` + `VERIFIED` |
| `{เงินรวม}` | เงินรวม 12 เดือน | `PC`, `PT`, `PQ` ที่ใช้ tag | computed | static mapping ตามตำแหน่ง ไม่คูณ 12 ใน runtime | `CONFIRMED` + `VERIFIED` |
| `{เงินรวมTEXT}` | คำอ่านเงินรวม 12 เดือน | `PC`, `PT`, `PQ` ที่ใช้ tag | computed | static mapping คู่กับเงินรวม | `CONFIRMED` + `VERIFIED` |
| `{บาทอากร}` | ตัวเลขฐานอากร | `PC`, `PT`, `PQ` ที่ใช้ tag | computed | เงินรวม / 1000 และส่งเฉพาะตัวเลข เพราะ template มีคำว่า “บาท” แล้ว | `CONFIRMED` + `VERIFIED` |
| `{สัญชาติ}`, `{เชื้อชาติ}`, `{ศาสนา}` | ข้อมูลส่วนตัว | `PQ` ที่ใช้ tag | optional | trim แยก field; ว่างให้จุดว่าง; จำกับ record ลูกจ้าง | `CONFIRMED` + `VERIFIED` |
| `{เกิดวันที่}`, `{เดือนเกิด}`, `{ปีเกิด}`, `{อายุลูกจ้าง}` | วันเกิดและอายุ | `PQ` ที่ใช้ tag | optional | วัน/เดือนเลือกจากรายการหรือกรอกเอง, ปีและอายุกรอกเอง; ไม่คำนวณอายุ; ว่างให้จุดว่าง | `CONFIRMED` + `VERIFIED` |
| `{เลขประจำตัว}` | เลขประจำตัวประชาชน; UI label “เลขประจำตัวประชาชน” | `PQ` ทั้ง 13 ไฟล์ และ `Template/9. ใบสั่งจ้าง.docx` | บังคับ; ตัวเลข 13 หลัก | เก็บเป็น string รักษา 0 นำหน้า ไม่ใส่ comma; ผิดความยาว/มีอักษรห้ามสร้าง | `CONFIRMED` + `VERIFIED` |
| `{ออกอำเภอ}`, `{ออกจังหวัด}` | สถานที่ออกบัตร | `PQ` ที่ใช้ tag | optional | รับเฉพาะชื่อ, trim และไม่เติมคำซ้ำ; ว่างให้จุดว่าง | `CONFIRMED` + `VERIFIED` |
| `{วันที่ออกบัตร}`, `{เดือนออกบัตร}`, `{ปีออกบัตร}` | วันออกบัตร | `PQ` ที่ใช้ tag | optional | กฎวัน/เดือน/ปีเดียวกับวันเกิด; ไม่แสดง “กรอกเอง”; ว่างให้จุดว่าง | `CONFIRMED` + `VERIFIED` |
| `{วันบัตรหมดอายุ}`, `{เดือนบัตรหมดอายุ}`, `{ปีบัตรหมดอายุ}` | วันหมดอายุบัตร | `PQ` ที่ใช้ tag | optional | กฎวัน/เดือน/ปีเดียวกับวันเกิด; ไม่แสดง “กรอกเอง”; ว่างให้จุดว่าง | `CONFIRMED` + `VERIFIED` |
| `{สำเร็จการศึกษาระดับ}` | ระดับการศึกษาที่สำเร็จ | `PQ` ที่ใช้ tag | optional | dropdown ตามรายการ context และกรอกเองได้; ว่างให้จุดว่าง | `CONFIRMED` + `VERIFIED` |
| `{คุณวุฒิการศึกษา}` | คุณวุฒิการศึกษา | `PQ` ที่ใช้ tag | optional | dropdown ตามรายการ context และกรอกเองได้; ไม่ส่งคำว่า “กรอกเอง”; ว่างให้จุดว่าง | `CONFIRMED` + `VERIFIED` |
| `{บ้านเลขที่ลูกจ้าง}`, `{ตำบลลูกจ้าง}`, `{อำเภอลูกจ้าง}`, `{จังหวัดลูกจ้าง}` | ที่อยู่ลูกจ้าง; UI ใช้ label ภาษาคน | `PQ` ทั้ง 13 ไฟล์ และ `Template/9. ใบสั่งจ้าง.docx`; Payroll ใบสำคัญรับเงินใช้ด้วย | ไม่บังคับ | trim/normalize แยก field; ว่างให้จุด; ชื่อพื้นที่ไม่รวม prefix ที่ template ใส่ไว้แล้ว; จำกับ record ร่วม | `CONFIRMED` + `VERIFIED`; แก้แถวเดิมที่ระบุผิดว่าไม่มีทั้งกลุ่ม |
| `{ถนนลูกจ้าง}` | ถนน; field ร่วมที่ต้องกรอก/จำได้ตั้งแต่ต้น | ไม่พบเฉพาะ tag นี้ใน Procurement; พบใน Payroll `8. ใบสำคัญรับเงิน.docx` | ไม่บังคับ | จำรายบุคคล; ว่างให้จุดเมื่อ Payroll render; ไม่เพิ่ม tag ลง Procurement | `CONFIRMED` + `VERIFIED`; เป็น compatibility field เพิ่มจาก Procurement 56 tags |

## Payroll — actual tags และ registry compatibility

ไฟล์ `PP` ที่ตรวจ: `1. หนังสือส่งเบิกจ้างเหมา.docx`, `3. ใบส่งมอบงาน.docx`, `4. ใบตรวจรับ.docx`, `5. บันทึกอนุมัติเบิกจ่าย.docx`, `8. ใบสำคัญรับเงิน.docx`

| Tag | ความหมาย / การจับคู่ | ไฟล์ `PP` ที่ใช้ | Required / empty | Save scope และ status |
|---|---|---|---|---|
| `{เดือนที่ส่งมอบ}`, `{ย่อเดือนที่ส่งมอบ}` | เดือนส่งมอบและรูปย่อ | `1`, `3`, `8`; ย่อเดือนมีใน `3`, `4` | workflow Payroll; ค่าคำนวณจากเดือนที่เลือก | งวด/preset Payroll; `VERIFIED` |
| `{วันที่ส่งเบิก}` | วันที่ส่งเบิก/วันที่อ้างอิงงวด | `1`, `3`, `4`, `5` | workflow Payroll; ว่างตามกฎเดิม | งวด/preset Payroll; `VERIFIED` |
| `{วันที่สั่งจ้าง}` | วันที่สั่งจ้าง | `3`, `4` | workflow Payroll; ว่างตามกฎเดิม | record/workflow Payroll; `VERIFIED` |
| `{ใบสั่งจ้าง}` | เลขที่ใบสั่งจ้าง | `3`, `4` | field งาน Payroll ตาม template | record/workflow Payroll; `VERIFIED` |
| `{ปีงบประมาณ}` | ปีงบประมาณ | `1`, `3`, `4` | computed จากเดือน/ปี พ.ศ. | ค่า computed; `VERIFIED` |
| `{ปีใบสั่งจ้าง}` | ชื่อ canonical เดิมใน registry/source สำหรับปีใบสั่งจ้าง ซึ่ง baseline alias ให้ค่าเดียวกับปีงบประมาณ | ไม่พบใน XML 5 ไฟล์; มีใน `template_tags.json` | ไม่ควรสร้าง tag ใหม่ใน DOCX โดยเดา | เก็บ alias เพื่อ compatibility แล้ว reconcile manifest; `VERIFIED` registry-only |
| `{คำนำหน้าลูกจ้าง}`, `{ชื่อลูกจ้าง}`, `{นามสกุลลูกจ้าง}` | ข้อมูลชื่อ record ลูกจ้าง | `1`, `3`, `4`, `5`, `8` ตาม tag ที่พบ | ชื่อ/นามสกุลเป็นข้อมูลหลัก; empty ใช้กฎ Payroll | record ลูกจ้างร่วม Payroll/Procurement; `CONFIRMED` + `VERIFIED` |
| `{คำนำหน้าชื่อ}` | tag ซ้ำความหมาย ต้อง resolve ตามชื่อบุคคลที่ติดกันใน paragraph เดียวกัน | `4. ใบตรวจรับ.docx` ใช้กับลูกจ้าง 1 จุด; `5. บันทึกอนุมัติเบิกจ่าย.docx` ใช้กับลูกจ้าง/พัสดุ/หัวหน้าพัสดุ/การเงิน อย่างละ 1 จุด | prefix ของเจ้าของชื่อนั้น; ห้าม alias ทุกจุดเป็นลูกจ้าง | ดูตาราง occurrence mapping ด้านล่าง; source เดิม alias ลูกจ้างทั้งไฟล์เป็นความเสี่ยงที่ตรวจพบ ไม่ใช่ mapping ที่ถูกต้องของทุกบทบาท |
| `{ชื่อโรงเรียน}` | ชื่อโรงเรียนของ record | `1`, `3`, `4`, `5`, `8` | บังคับตาม workflow Payroll | record การจ้างรายบุคคล; normalize school; `CONFIRMED` + `VERIFIED` |
| `{ตำแหน่ง}` | ตำแหน่งลูกจ้าง | `1`, `3`, `4`, `5`, `8` | บังคับตาม workflow Payroll | record/workflow; `VERIFIED` |
| `{เงินเดือน}`, `{เงินเดือนTEXT}` | เงินเดือนและคำอ่าน | `3`, `4`, `5`, `8` | computed/required ตามเอกสาร | source จากตำแหน่ง; `VERIFIED` |
| `{กรรมการA}`, `{กรรมการB}`, `{กรรมการC}` | ชื่อเต็มกรรมการตรวจรับ Payroll | `4. ใบตรวจรับ.docx` | กรรมการ A/B/C ชุดเดียวกับ TOR; A ประธาน | สร้างจาก source กรรมการเดียวกับ spec tags: A→A/B→B/C→C; `CONFIRMED` + `VERIFIED` |
| `{คำนำหน้าผอ}`, `{ชื่อผอ}`, `{นามสกุลผอ}` | ผู้ลงนามผู้อำนวยการ | `1`, `3`, `5` ตาม tag ที่พบ | ตาม workflow Payroll | ข้อมูลโรงเรียน/ผู้ลงนาม; `VERIFIED` |
| `{คำนำหน้าพัสดุ}`, `{ชื่อพัสดุ}`, `{นามสกุลพัสดุ}` | เจ้าหน้าที่พัสดุ | `3`, `5` ตาม tag ที่พบ | ตาม workflow Payroll | ข้อมูลผู้ลงนาม; `VERIFIED` |
| `{ชื่อหพัสดุ}`, `{นามสกุลหพัสดุ}` | ชื่อ/นามสกุลหัวหน้าพัสดุ | `5. บันทึกอนุมัติเบิกจ่าย.docx` | ตาม workflow Payroll | ผู้ลงนามของ record; prefix ของบุคคลนี้ส่งผ่าน occurrence `{คำนำหน้าชื่อ}` ใน paragraph เดียวกัน; `VERIFIED` |
| `{ชื่อการเงิน}`, `{นามสกุลการเงิน}` | เจ้าหน้าที่การเงิน | `5. บันทึกอนุมัติเบิกจ่าย.docx` | ตาม workflow Payroll | ผู้ลงนาม Payroll ของ record; prefix ผ่าน occurrence `{คำนำหน้าชื่อ}` ข้างชื่อการเงิน; ไม่ใช่คนเดียวกับ optional head finance โดยอัตโนมัติ |
| `{โซนหัวหน้าการเงิน}`, `{หัวหน้าการเงิน}` | ชื่อเต็มผู้ลงนามหัวหน้าการเงิน optional และข้อความตำแหน่ง | `5. บันทึกอนุมัติเบิกจ่าย.docx` | optional ตาม baseline; ไม่มีหัวหน้าให้ทั้งคู่เป็น empty string ไม่เติมจุด | Payroll checkbox/input ของ record; มีหัวหน้าจึง compose ชื่อเต็มและส่งตำแหน่ง `หัวหน้าเจ้าหน้าที่การเงิน`; `VERIFIED` |
| `{คำนำหน้าการเงิน}` | canonical prefix เจ้าหน้าที่การเงินตาม registry | ไม่มี tag สะกดนี้ใน XML; ส่งค่าไป occurrence `{คำนำหน้าชื่อ}` ข้าง `{ชื่อการเงิน}` ในไฟล์ `5` | ต้องรักษา input สำหรับผู้ลงนามนี้ | registry-only ในเชิง spelling แต่ค่ามีผู้ใช้จริงผ่าน contextual alias; อย่าลบ field |
| `{คำนำหน้าหพัสดุ}` | canonical prefix หัวหน้าพัสดุตาม registry | ไม่มี tag สะกดนี้ใน XML; ส่งค่าไป occurrence `{คำนำหน้าชื่อ}` ข้าง `{ชื่อหพัสดุ}` ในไฟล์ `5` | ต้องรักษา input สำหรับผู้ลงนามนี้ | registry-only ในเชิง spelling แต่ค่ามีผู้ใช้จริงผ่าน contextual alias; อย่าลบ field |
| `{บ้านเลขที่ลูกจ้าง}`, `{ถนนลูกจ้าง}`, `{ตำบลลูกจ้าง}`, `{อำเภอลูกจ้าง}`, `{จังหวัดลูกจ้าง}` | ที่อยู่ลูกจ้าง; UI ใช้ label ภาษาคน โดยเฉพาะ `ถนน` | `8. ใบสำคัญรับเงิน.docx` | ทุกช่อง optional; ว่างให้จุดว่าง | record ลูกจ้างร่วม; `{ถนนลูกจ้าง}` ต้องมีช่องตั้งแต่ต้น; `CONFIRMED` + `VERIFIED` |

## กฎ mapping ข้ามแท็บ

1. Payroll `{กรรมการA/B/C}` เป็นชื่อเต็มจากกรรมการ source เดียวกับ Procurement `{คำนำหน้าสเปคA/B/C}`, `{ชื่อสเปคA/B/C}`, `{นามสกุลสเปคA/B/C}`, `{ตำแหน่งสเปคA/B/C}` โดย A→A, B→B, C→C และ A เป็นประธานเสมอ ไม่อ่านค่าจากไฟล์ output ที่สร้างแล้ว
2. Payroll `{คำนำหน้าชื่อ}` ต้องรับ prefix ของบุคคลตาม occurrence mapping ด้านล่าง; ใช้ prefix ลูกจ้างเฉพาะจุดที่ติดกับ `{ชื่อลูกจ้าง}`
3. `{ถนนลูกจ้าง}` เป็น field เดียวใน record ลูกจ้าง ป้ายบน UI คือ `ถนน`; Payroll `8. ใบสำคัญรับเงิน.docx` ใช้ค่าได้ ส่วน Procurement ไม่มี tag นี้และไม่ต้องเพิ่ม tag ลง template
4. Tag ที่ไม่มีในไฟล์ที่เลือกต้องไม่ทำให้ค่าจากอีกแท็บหรือ record อื่นรั่วเข้าเอกสาร; renderer แทนเฉพาะ tags ที่พบใน package และตรวจ body/header/footer ทุกส่วน
5. ค่าว่างของ optional fields ใช้จุดว่างตามกฎของ context; ห้ามส่งข้อความ `(กรอกเอง)` ลง DOCX

## Save scope และสถานะที่ implementation ต้องรักษา

- ข้อมูลลูกจ้างรายบุคคล: ชื่อ คำนำหน้า เลขประจำตัว ที่อยู่รวมถนน วันเกิด/บัตร/การศึกษา และข้อมูลการจ้างที่ผูกกับโรงเรียน; เครื่องเดียวมีหลายโรงเรียนได้
- ข้อมูลกรรมการ A/B/C เป็นแหล่งข้อมูลร่วมของสองแท็บ แก้ที่หนึ่งแล้วอีกแท็บเห็นทันที ไม่สร้างชุดกรรมการซ้ำคนละบทบาท
- `{คำสั่งสเปค}` จำกับ record การจ้างรายบุคคลและว่างได้
- งวด/เดือน/quick-load/preset เป็นข้อมูลเฉพาะ Payroll; routing, flags และคำสั่ง TOR เป็นข้อมูลเฉพาะ Procurement เว้นแต่ field ร่วมที่ระบุข้างต้น
- “เพิ่มลูกจ้างใหม่” ถามก่อนบันทึก Template ตามชื่อ–นามสกุล ใช้ flow เดียวกับปุ่มบันทึก Template แล้วล้างฟอร์มของ record ปัจจุบันทั้งหมดไม่ว่าผู้ใช้จะบันทึกหรือไม่
- โหลด Template ต้องแจ้งว่าเป็นการนำข้อมูลมาใช้กับฟอร์มและไม่แก้ record เดิม จนกว่าจะสั่งบันทึกทับชื่อเดิม

## จุดตรวจที่ implementation ต้องทำต่อ

- ปรับ manifest ของ Payroll ให้สอดคล้องกับ XML จริงโดยคง alias ที่ระบุ ไม่ประกาศความหมายของ tag registry-only เป็น tag ใน DOCX ใหม่โดยไม่มี template รองรับ
- สร้าง values dictionary แยก canonical field กับ output alias แล้วทดสอบ replacement ใน body/header/footer และ split runs
- ตรวจว่า generated DOCX ไม่มี business tag ค้างและไม่แทน GUID content control IDs เป็นข้อมูลผู้ใช้

## Astra review — mapping ที่ต้องพิจารณาราย occurrence

หลักฐาน: `word/document.xml` ของ Payroll `5. บันทึกอนุมัติเบิกจ่าย.docx` มีข้อความสี่ชุดนี้จริง; เป็นการแก้ความหมายใน handoff จาก XML ไม่ได้เปลี่ยน Word template หรือเพิ่มบทบาทใหม่

| บริบท tag เดิม | ค่า `{คำนำหน้าชื่อ}` ที่ต้องใช้ |
|---|---|
| `{คำนำหน้าชื่อ}{ชื่อลูกจ้าง} {นามสกุลลูกจ้าง}` | prefix ลูกจ้าง |
| `{คำนำหน้าชื่อ}{ชื่อพัสดุ} {นามสกุลพัสดุ}` | prefix เจ้าหน้าที่พัสดุ |
| `{คำนำหน้าชื่อ}{ชื่อหพัสดุ} {นามสกุลหพัสดุ}` | prefix หัวหน้าพัสดุ |
| `{คำนำหน้าชื่อ}{ชื่อการเงิน} {นามสกุลการเงิน}` | prefix เจ้าหน้าที่การเงิน |

Payroll ไฟล์ `5` ยังมี `โรงเรียน{ชื่อโรงเรียน}` 2 จุด แต่ `{ชื่อโรงเรียน}` อีก 3 จุดต้องการชื่อเต็ม จึงใช้ dictionary ค่าเดียวทั้งไฟล์ไม่ได้: เก็บ canonical school name แบบ normalize แล้ว และใช้ชื่อที่ตัด prefix เฉพาะ occurrence ที่ template มีคำว่า `โรงเรียน` อยู่หน้าช่องแล้ว ห้าม replace คำว่าโรงเรียนซ้ำทั้งเอกสารหลัง render เพราะอาจแก้เนื้อหาคงที่อื่น

แก้ที่ mapping ของสำเนาในหน่วยความจำระหว่าง generate โดยผูก module + relative path + บริบท tag ข้างเคียงใน paragraph; ไม่ยึดเลข run/paragraph ที่เปลี่ยนง่าย ถ้าบริบทไม่ตรงหรือกำกวมให้แจ้ง mapping error ของไฟล์นั้น ไม่เดาบุคคล ห้ามบันทึกการเปลี่ยน alias กลับไปยัง DOCX ต้นทาง

กรรมการเก็บ A/B/C เป็น object ที่มี prefix, given name, surname, position แยกกัน และ compose ชื่อเต็มเฉพาะตอน Payroll render; ห้าม parse ชื่อเต็มกลับเพื่อเป็นแหล่งข้อมูลหลัก เพราะ custom prefix/ชื่อมีช่องว่างอาจเสียข้อมูล

Required สำหรับ Procurement ยังคงชื่อ/นามสกุล/เลขประชาชน/โรงเรียน/ตำแหน่ง/กรรมการครบก่อน generate แม้เลือกเอกสารบางไฟล์; conditional school-two ตาม flag; input ผู้ลงนามที่เอกสารใช้ต้องมีตามบทบาท โดย Payroll คง validation baseline ของตนและไม่ถูกบังคับเลขประชาชน/TOR position ใหม่ การบันทึก Template ที่ยังกรอกไม่ครบไม่ใช้ generate validation มาขวาง
