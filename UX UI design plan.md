# เอกสาร 346 — UX UI design plan

วันที่: 2026-09-09 | สถานะ: ทิศทางที่ยืนยันแล้ว; อยู่ระหว่างแก้ UI
ใช้คู่กับ PROJECT_CONTEXT.md, ARCHITECTURE_PLAN.md และ IMPLEMENTATION_HANDOFF.md
ข้อกำหนดธุรกิจและคำตอบล่าสุดของผู้ใช้มีลำดับเหนือข้อเสนอด้านหน้าตาในไฟล์นี้

**คำตอบที่ยืนยันล่าสุด:** ใช้หน้าตาและภาษาภาพของโปรแกรมเดิมเป็นฐาน โดยเปลี่ยนเฉพาะการจัดวางข้อมูลให้เหมาะกับ workflow ของ Procurement; ใช้ accent สีเขียวสบายตาสำหรับจัดซื้อจัดจ้างและสัญญา และคง accent สีฟ้าสำหรับเบิกเงินเดือน ทั้งสองแท็บต้องใช้ header, card, เส้นแบ่งหัวข้อ, spacing, ปุ่ม และลำดับโซนจาก design system เดียวกัน

อ่าน `ASTRA_REVIEW.md` ควบคู่ด้วย ปุ่มบันทึก/โหลด Template ใช้ snapshot ของ record ทั้งชุดจากสองแท็บ โรงเรียนและกรรมการเป็นข้อมูลของลูกจ้างรายนี้ ไม่ใช่โปรไฟล์ global ที่คงอยู่ให้ลูกจ้างคนถัดไป

เพิ่มเติม 2026-09-08: หน้าต่างหลักมีแท็บบนสุด `[เบิกเงินเดือน] [จัดซื้อจัดจ้างและสัญญา]` ตาม ARCHITECTURE_PLAN ส่วน Procurement ใช้ 4 หมวด: โรงเรียน, กรรมการ TOR, ข้อมูลลูกจ้างแบบรวมตำแหน่ง/ที่อยู่/บัตร/การศึกษา และสรุปชุดเอกสาร แท็บเบิกเงินเดือนรักษา workflow เดิม รวมเดือนส่งมอบ/preset/เลื่อนเดือน การเปิดโปรแกรมเริ่มที่แท็บ “จัดซื้อจัดจ้างและสัญญา” ตามคำตอบผู้ใช้ การสลับแท็บต้องคงข้อมูลรายบุคคลร่วมกันและสะท้อนการแก้ไขข้อมูลร่วมทันที ข้อมูลเฉพาะ workflow ปุ่มสร้าง และสถานะยังเป็นของแท็บนั้น

## 1. ทิศทางและตัวอย่างจาก web search

เลือกแนว **โปรแกรม Windows สีอ่อน ฟอร์มอ่านง่าย แยกหมวดชัด** สำหรับเจ้าหน้าที่โรงเรียนที่กรอกข้อมูลและสร้างเอกสารหลายคนต่อเนื่อง เป้าหมายคือรู้ว่ากำลังทำข้อมูลใคร เหลืออะไร และสร้างเอกสารได้โดยไม่ต้องจำชื่อ template

| ตัวอย่าง / แหล่งอ้างอิง | สิ่งที่นำมาประยุกต์ |
|---|---|
| [WinUI Gallery — ตัวอย่างโปรแกรมของ Microsoft](https://github.com/microsoft/WinUI-Gallery) และ [ภาพ Gallery ในเอกสาร Microsoft](https://learn.microsoft.com/zh-cn/windows/apps/winui/winui3/) | ใช้เป็นภาพอ้างอิงภาพรวมของโปรแกรม Windows; เลือกโครงเมนูด้านซ้ายและพื้นที่ทำงานด้านขวา ไม่ต้องทำหน้ารวมการ์ดแบบ Gallery |
| [NavigationView — ตัวอย่างเมนูซ้ายและการปรับตามขนาดหน้าต่าง](https://learn.microsoft.com/en-us/windows/apps/develop/ui/controls/navigationview) | หมวดที่เลือกต้องเห็นชัด และมีทางเข้าหมวดอื่นได้ตลอด |
| [Microsoft Forms — มีภาพฟอร์มและตัวอย่าง Customer Orders Database](https://learn.microsoft.com/en-us/windows/apps/develop/ui/controls/forms) | label เหนือช่อง, จัด field ที่สัมพันธ์กันเป็นกลุ่ม, ใช้จำนวนคอลัมน์ตามพื้นที่ |
| [Fluent 2 Field — ตัวอย่าง label/helper/error](https://fluent2.microsoft.design/components/web/react/core/field/usage) | ข้อความช่วยใต้ช่องและ error ที่บอกวิธีแก้ ไม่ใช้ placeholder แทน label |

ทั้งหมดเป็นต้นแบบด้านการจัดหน้าและพฤติกรรม ไม่ใช่ข้อกำหนดให้ย้ายไป WinUI/React ค่าขนาด สี และ workflow ด้านล่างเป็นข้อเสนอเฉพาะโครงการ 346 ไม่ใช่ค่ามาตรฐานที่คัดจากเว็บ

## 2. โครงหน้าจอหลัก

แสดงฟอร์มทีละหมวด สลับหมวดได้โดยค่าที่กรอกยังอยู่ ไม่มี wizard ที่บังคับกดถัดไปทุกครั้ง

```text
┌──────────────────────────────────────────────────────────────────┐
│ เอกสาร 346         โรงเรียน: [ชื่อโรงเรียน]      ปีงาน: 2570       │
│ ลูกจ้าง: [ชื่อผู้ที่กำลังแก้]                  มีการแก้ไขยังไม่บันทึก │
├─────────────────┬────────────────────────────────────────────────┤
│ ข้อมูลที่ใช้ร่วม  │ 3. ข้อมูลลูกจ้าง                                │
│ โรงเรียน/กรรมการ  │ ช่องที่มี * จำเป็นสำหรับสร้างเอกสาร               │
│ ของ record นี้     │                                                │
│                 │ คำนำหน้า          ชื่อ *         นามสกุล *      │
│ ลูกจ้างรายนี้     │ [เลือก/พิมพ์]     [_________]   [_________]    │
│ 3 ข้อมูลลูกจ้าง ◀ │                                                │
│ 4 สรุปชุดเอกสาร  │ เลขประจำตัวประชาชน *                           │
│   ที่ต้องการสร้าง │ [____________________]                         │
│                 │ [โหลดลูกจ้าง] [บันทึกลูกจ้าง] [เพิ่มลูกจ้างใหม่]  │
├─────────────────┴────────────────────────────────────────────────┤
│ ยังขาดข้อมูล 2 รายการ [ดูรายการ]      [ตรวจสอบและสร้างเอกสาร]     │
└──────────────────────────────────────────────────────────────────┘
```

- แถบหัว: ชื่อโปรแกรม “เอกสาร 346”, โรงเรียนปัจจุบัน, ลูกจ้างปัจจุบัน และสถานะบันทึก; ยังไม่เลือกลูกจ้างใช้ “ลูกจ้างใหม่”
- เมนูซ้าย: ชื่อหมวดภาษาไทยพร้อมเลข 1–4; ข้อมูลตำแหน่ง/ที่อยู่/บัตร/การศึกษารวมอยู่กับข้อมูลลูกจ้าง ลดการสลับหมวด
- เนื้อหา: ชื่อหมวด → คำอธิบายสั้น → ฟอร์ม → ปุ่มของหมวด; scroll เฉพาะเนื้อหา ปุ่มหลักไม่หลุดจอ
- แถบล่าง: สถานะความครบของข้อมูลและปุ่มหลักสีฟ้าหนึ่งปุ่ม เมื่อพร้อมใช้ “ตรวจสอบและสร้างเอกสาร” แล้วพาไปสรุปก่อนสั่งสร้างจริง
- ไม่แสดง path Template, tag, schema หรือหมายเลข routing ในฟอร์มหลัก ย้ายรายละเอียดเทคนิคไปกล่องรายละเอียดเมื่อเกิดปัญหา

## 3. เนื้อหาแต่ละหมวด

| หมวด | การจัดหน้าและพฤติกรรม |
|---|---|
| 1 โรงเรียน | ชื่อโรงเรียน/เขตอยู่บนสุด; บุคลากรเดิมแยกกลุ่มตามหน้าที่; เขตแยกประเภทและ suffix พร้อมตัวอย่างชื่อเต็ม ปุ่ม “จัดการรายการเขต” อยู่ข้าง dropdown |
| 2 กรรมการ TOR | กลุ่มกรรมการ 1/2/3 เรียงลงมา กรรมการ 1 เป็นประธาน แต่ละกลุ่มมีกรอบ/เส้นแบ่งชัดเจน; ปิดท้ายด้วย `เลขที่คำสั่งแต่งตั้ง TORและตรวจรับ`; ใช้ชุดข้อมูลร่วมกับกรรมการตรวจรับ Payroll |
| 3 ข้อมูลลูกจ้าง | รวมข้อมูลส่วนตัว → ตำแหน่ง/งานสอน/สองโรงเรียน/โรงเรียนที่สอง → เงินเดือน/เงินรวม 12 เดือน/ค่าอากรแสตมป์ → ที่อยู่/บัตร/การศึกษา; อายุแสดงหน่วย “ปี” และมี note เรื่องช่องว่างแทนด้วยจุด |
| 4 สรุปชุดเอกสาร | แสดงเฉพาะรายการเอกสารที่จะสร้าง เปลี่ยนหัวข้อเป็น `สรุปชุดเอกสารที่ต้องการสร้าง` |

วัน/เดือนต้องใช้ dropdown + ตัวเลือกกรอกเอง ปีและอายุกรอกเองตาม context ไม่เปลี่ยนเป็น date picker ที่บังคับวันที่ครบ รายการการศึกษาและคำนำหน้าใช้ค่าที่ context ระบุ

ปุ่มลูกจ้างวางตำแหน่งเดียวกันในหมวด 3–5 ส่วน “ล้างข้อมูลหมวดนี้” เป็นปุ่มรองแยกจากปุ่มบันทึก พร้อมบอกชื่อหมวดก่อนล้าง หากมีข้อมูลค้างก่อนปิดโปรแกรม ให้เลือก `บันทึก` หรือ `ไม่บันทึก`; กากบาทของกล่องถามใช้ปิดกล่องและกลับไปทำงานต่อ ไม่เพิ่มปุ่มข้อความ “ยกเลิกการปิด”

## 4. สรุปก่อนสร้างและผลลัพธ์

1. กดตรวจสอบ → validate; ถ้าข้อมูลผิด แสดงจำนวนข้อผิดพลาดและรายการที่กดพาไป field ได้ ยังไม่สร้างไฟล์
2. ถ้าผ่าน เปิดกล่องสรุป: โรงเรียน, ลูกจ้าง, ตำแหน่ง, งานสอน/2 โรงเรียน, เงินเดือน/เงินรวม/อากร, รายการเอกสารและโฟลเดอร์ปลายทาง
3. สรุปอ่านจาก routing/model เดียวกับ generator ชุดเต็มคือ 10 ไฟล์ รวมขั้นตอน 2.1 และ 2.2 ไม่ใช่ 9 ไฟล์; รายการเริ่มต้นติ๊กครบทุกไฟล์แต่ผู้ใช้ยกเลิกเลือกบางไฟล์ได้ และสรุปต้องแสดงจำนวนตามที่เลือกจริง
4. ปุ่ม “กลับไปแก้ไข” และ “สร้างเอกสาร” เมื่อกดสร้าง ป้องกันการกดซ้ำและแก้ข้อมูลระหว่างสร้าง แสดง “กำลังสร้าง 4/10 ไฟล์” ตาม progress จริง
5. สำเร็จ: “สร้างเอกสารครบ N ไฟล์แล้ว” ตามจำนวนที่เลือกและตรวจสำเร็จจริง (ชุดเต็ม 10) พร้อม “เปิดโฟลเดอร์” และ “เพิ่มลูกจ้างใหม่”
6. ไม่สำเร็จ: บอกไฟล์/สาเหตุที่แก้ได้ เช่น “ไม่สามารถบันทึกไฟล์ได้ กรุณาปิดไฟล์ Word แล้วลองใหม่” เฉพาะเมื่อทราบว่าเป็น file lock; สาเหตุอื่นต้องใช้ข้อความตามจริง คงข้อมูลที่กรอกและไม่แสดงว่าสร้างครบ

ไม่มี preview Word ฝังในโปรแกรมในรอบนี้ ใช้สรุปข้อมูลและเปิดไฟล์ภายนอกเพื่อคงขอบเขตเดิม

## 5. รูปแบบภาพที่แนะนำ

คงฐานภาพของโปรแกรมเดิม: header สีประจำ workflow, card สีขาวมีเส้นขอบบาง, หัวข้อกลุ่มที่มองเห็นเป็นแถบ/เส้นแบ่ง และปุ่มหลักที่ anchored อยู่ใน command bar เดียวกัน เปลี่ยนสี accent ตามแท็บโดยไม่เปลี่ยนโครงหรือภาษา visual

| รายการ | ค่าเริ่มต้นสำหรับ implementation |
|---|---|
| พื้นโปรแกรม / พื้นฟอร์ม | `#F8F5FF` / `#FFFFFF` |
| ตัวอักษร / ข้อความรอง | `#1F2937` / `#526174` |
| Procurement accent | `#4C956C` ตัวอักษรขาว / พื้น `#EAF5EE` ตัวอักษร `#25613E` |
| Payroll accent | `#5397E2` ตัวอักษรขาว / พื้น `#EAF3FF` ตัวอักษร `#2F5FAD` |
| เส้นแบ่งกลุ่ม / ขอบช่องกรอก | `#D8DEE8` / `#7A8698` |
| ผิดพลาด / สำเร็จ | `#B42318` / `#166534` พร้อมข้อความหรือสัญลักษณ์ ไม่ใช้สีอย่างเดียว |
| Font | คง Tahoma เพื่อภาษาไทยและความต่อเนื่อง: เนื้อหา 10.5–11 pt, หัวกลุ่ม 12 pt bold, หัวหน้า 16–18 pt bold |
| ช่องว่าง | ฐาน 8 logical px; ระหว่างช่อง 12–16, ระหว่างกลุ่ม 24, ขอบเนื้อหา 24 |
| ปุ่ม / ช่องกรอก | เป้าความสูงปุ่ม 36–40 logical px; ช่องกรอกตาม preferred size ของ native control + ระยะว่าง ไม่ฝืนความสูงจนสระไทยถูกตัด |

ใช้ native title bar, focus indicator และ controls เป็นหลัก ไม่ทำเงาหนัก gradient glass effect, emoji navigation หรือ custom drawing จำนวนมาก ไม่เพิ่ม dependency เพียงเพื่อมุมโค้ง

## 6. โครงโซนที่ใช้ร่วมกัน

- Header ด้านบน: ชื่อ workflow, คำอธิบายสั้น และสถานะ ใช้สีของแท็บนั้น
- Workspace: ซ้ายเป็นรายการขั้นตอน/เอกสารความกว้างคงที่ประมาณ 220–260 px; ขวาเป็นพื้นที่กรอกข้อมูลที่ขยายได้
- Card ในพื้นที่กรอก: พื้นขาว ขอบบาง หัวข้อกลุ่มมีแถบหรือเส้นแบ่งชัดเจน และใช้ระยะห่าง 16/24 px ตามลำดับชั้น
- Command bar ด้านล่าง: ปุ่มล้างอยู่ซ้าย ปุ่มบันทึก/โหลดเป็นรอง และปุ่มสร้างเอกสารเป็น primary อยู่ขวา
- เมื่อพื้นที่แคบ ให้ลดเมนูซ้ายและ reflow เนื้อหาเป็นหนึ่งคอลัมน์ก่อนเกิด horizontal clipping; ไม่ปล่อยให้ฟอร์มถูกบีบไปอยู่ขอบจอ

## 7. ขนาดหน้าต่างและการใช้คีย์บอร์ด

- เริ่มจากหน้าต่างประมาณ 1120 × 740 ที่ 100% DPI แต่ต้องจำกัดให้ไม่เกิน WorkingArea ของจอ ไม่คง minimum 980 × 700 แบบตายตัวจนล้นจอเมื่อ scaling สูง
- พื้นที่ client กว้างตั้งแต่ประมาณ 1000 logical px: เมนูซ้าย 200–220 px และฟอร์มไม่เกิน 2 คอลัมน์; ชื่อ/นามสกุลและวัน/เดือน/ปีเป็นกลุ่มย่อยที่จัดติดกันได้
- เมื่อแคบ: เปลี่ยนเมนูซ้ายเป็น dropdown “หมวดข้อมูล” ที่ด้านบน ฟอร์มหนึ่งคอลัมน์ และให้ปุ่มแถบล่างขึ้นสองแถวได้ หลีกเลี่ยงแถบเมนูที่เหลือแค่ icon
- ตั้ง TabIndex ตามลำดับที่อ่าน, Tab/Shift+Tab ครบ, Space ติ๊ก checkbox, keyboard เปิด dropdown ได้ และ Esc ปิดกล่องสรุปโดยไม่ล้างข้อมูล
- error ใต้ field พร้อม ErrorProvider; กดสร้างเมื่อไม่ครบให้พา focus ไปจุดแรก ผู้ใช้ต้องรู้เหตุผล ไม่ปล่อยปุ่ม disabled โดยไม่มีข้อความอธิบาย
- แสดง validation เมื่อออกจากช่องหรือกดตรวจสอบ ไม่ขึ้นแดงทุกช่องตั้งแต่เปิดโปรแกรม และไม่เด้ง MessageBox ทุกตัวอักษร
- ตั้ง AccessibleName ตาม label จริงและใช้สีระบบเมื่อ high contrast; ตรวจ contrast ตัวอักษรและขอบ/focus ในหน้าจอจริง

## 7. จุดที่ต้องรักษาตามคำตอบที่ยืนยันแล้ว

- D01 ยืนยันแล้ว: record ลูกจ้างแต่ละรายระบุ `{ชื่อโรงเรียน}` ได้ และเครื่องเดียวมีหลายโรงเรียนได้ ไม่ต้องมี active-school หรือเมนูสลับโรงเรียนในรุ่นนี้
- D02 ยืนยันแล้ว: ข้อมูลผู้ใช้แยกตามบัญชี Windows ใน `%LOCALAPPDATA%`; UI ต้องสื่อสถานะตาม store จริง ไม่อ้างว่าแชร์ทุก user
- D03 ยืนยันแล้ว: default เขตลำดับที่ 2 คือ `สำนักงานเขตพื้นที่การศึกษาประถมศึกษาประจวบคีรีขันธ์ เขต 2`
- D04 ยืนยันแล้ว: รุ่นนี้เริ่มข้อมูลใหม่ ไม่แสดงหรือนำเข้า record เก่าจาก `saved_templates.json` baseline
- D05 ยืนยันแล้ว: `{คำสั่งสเปค}` แสดงในโซนกรรมการด้วยป้าย `เลขที่คำสั่งแต่งตั้ง TORและตรวจรับ`; ว่างได้และแสดงจุดในเอกสาร; ถ้ากรอกให้จำกับ record การจ้างรายบุคคล
- D06 ยืนยันแล้ว: แสดงช่องป้าย `ถนน` ตั้งแต่หมวดข้อมูลส่วนตัว/ที่อยู่ของลูกจ้าง กรอกได้แต่ไม่บังคับและจำรายบุคคล; ชื่อ `{ถนนลูกจ้าง}` ใช้เฉพาะเป็น tag ภายใน; Payroll ใช้ค่าได้ ส่วน Procurement ไม่ส่งออก tag นี้
- “เพิ่มลูกจ้างใหม่” ต้องถามก่อนว่าจะบันทึก Template ของชื่อ–นามสกุลปัจจุบันหรือไม่; ถ้าเลือกบันทึกให้ใช้ flow เดียวกับปุ่มบันทึก Template และไม่ว่าผลจะบันทึกหรือไม่ ให้ล้างข้อมูลฟอร์มของ record ปัจจุบันทั้งหมดแล้วจึงเริ่ม record ใหม่
- ตอนโหลด Template ต้องมีข้อความชัดเจนว่าโหลดมาใช้กับฟอร์มไม่กระทบข้อมูลเดิม เว้นแต่ผู้ใช้เลือกบันทึกทับชื่อข้อมูลเดิม
- เปิดโปรแกรมใหม่ต้องเริ่มเป็นฟอร์มว่างและไม่กู้คืนร่างอัตโนมัติ
- ค่าเริ่มต้นโฟลเดอร์ผลลัพธ์แยกเป็น `Output/เงินเดือน` และ `Output/เอกสารจัดจ้าง`; หากชื่อไฟล์ซ้ำให้ต่อท้าย `_2`, `_3` อัตโนมัติ
- การสลับหมวดเฉย ๆ ไม่บันทึกหรือล้างค่าอัตโนมัติ
- บันทึกข้อมูลกับสร้างเอกสารเป็นคนละผลลัพธ์ อย่าแสดง “บันทึกแล้ว” เพียงเพราะสร้างไฟล์สำเร็จ

### Flow ที่ชัดเจนหลัง Astra review

- ใช้คำว่า `บันทึก Template`/`โหลด Template` ให้สอดคล้องกันทั้งสองแท็บ และอธิบายว่าเป็นข้อมูลของลูกจ้าง ไม่ใช่ DOCX แม่แบบ; การบันทึกไม่ต้องรอข้อมูลครบตาม generate validation
- เพิ่มใหม่เมื่อเลือกบันทึกและบันทึกสำเร็จ หรือเลือกไม่บันทึก จึงล้าง record ทั้งสองแท็บรวมโรงเรียน/ผู้ลงนาม/กรรมการ/flags/โรงเรียนสอง/ข้อมูลเดือนของคนเดิม แล้วกลับค่าเริ่มต้นของแต่ละ workflow; คง custom dropdown lists/output preference ไว้
- X/ยกเลิกในกล่องคำถามหรือตั้งชื่อ/เขียนไฟล์ไม่ได้ให้กลับฟอร์มเดิมพร้อมข้อมูลครบ ไม่ตีความว่าผู้ใช้เลือกไม่บันทึก ไม่เพิ่มปุ่มชื่อ “ยกเลิกการปิด”
- โหลดให้แทน working copy ทั้งชุด ไม่ merge เฉพาะช่องที่มีจนเหลือข้อมูลคนเก่า ข้อความกำกับ: “นำข้อมูล Template มาใช้ในฟอร์ม การแก้ไขจะไม่เปลี่ยนข้อมูลที่บันทึกไว้จนกว่าจะบันทึกทับ”
- Quick Payroll แยกข้อความเรื่องงวดล่าสุด: successful generation อัปเดตประวัติงวดตาม baseline โดยไม่บันทึกฟอร์มทับเอง; เปลี่ยนคน/เพิ่มใหม่ต้องไม่มี quick binding ของคนก่อน
- ปิดโปรแกรมตรวจข้อมูลค้างของ shared record และสองแท็บร่วมกันครั้งเดียว; Save ต้องเก็บ input ทั้งสองแท็บสำเร็จก่อนปิด ไม่ให้บันทึกเฉพาะแท็บที่เปิดอยู่
- “ฟอร์มว่างเมื่อเปิดใหม่” หมายถึงไม่มีข้อมูลคนเก่าหรือร่างเก่าถูกโหลดเอง; ค่าเริ่มต้นระบบเช่นรายการ dropdown/เดือน Payroll/checkbox เอกสารยังมีได้และไม่ทำให้ขึ้น dirty ทันที

## 8. คำสั่งและเกณฑ์รับงานสำหรับ implementation

```text
ใช้ UX UI design plan.md เป็นแบบจัดหน้าในงาน T04 โดยอ่าน PROJECT_CONTEXT และ decision ล่าสุดก่อน
สร้าง layout shell, field groups และ states ด้วย WinForms เดิม ใช้ TableLayoutPanel/FlowLayoutPanel,
Dock/Anchor และ AutoScroll ตามความเหมาะสม แทนการเพิ่มพิกัดตายตัวทั่วฟอร์ม
เชื่อมกับ model, validation และ routing ใน architecture เดิม ไม่คัดลอก logic ลงใน UI ซ้ำ
เริ่ม shell + หมวดลูกจ้างหนึ่งหมวด ตรวจภาษาไทย/keyboard/DPI แล้วใช้ pattern เดียวกันกับหมวดที่เหลือ
ไม่สร้าง backend/framework ใหม่ ไม่แก้ template เพื่อให้เข้าหน้าตา และไม่ขยายฟีเจอร์จากรูปอ้างอิง
```

ตรวจรับหนึ่งรอบรวม: จอ 1366×768 ที่ 100% และ 1920×1080 ที่ 125%/150%, หน้าต่างแคบ, ชื่อไทยยาว, ตัวอักษร/สระไม่ตัด, ไม่มีช่อง/ปุ่มซ้อน, keyboard ครบ, ข้อมูลค้างเมื่อสลับหมวด/แท็บ, shared fields แก้แล้วเห็นอีกแท็บทันที, error พาไปแก้ได้, flags/โรงเรียนสองถูกต้อง; เพิ่มลูกจ้างใหม่หลัง Saved/Declined ต้องล้างข้อมูล record รวมโรงเรียน/กรรมการทั้งสองแท็บ ส่วน cancelled/failed save ต้องคงข้อมูลเดิมทั้งหมด

เก็บภาพหน้าจอจริงของฟอร์มปกติ, ข้อมูลไม่ครบ, สรุปก่อนสร้าง และผลสำเร็จประกอบ handoff ถ้ายังไม่ได้รัน UI ให้ระบุว่าเป็นแผนหรือยังไม่ได้ตรวจ ไม่ถือว่า ASCII wireframe นี้เป็นหลักฐาน visual QA
